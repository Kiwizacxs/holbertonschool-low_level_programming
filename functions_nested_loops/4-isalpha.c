#include "maina.h"

/**
* _isalpha - 1234
* Return: always 0.
*/

int _isalpha(int c)
{
	int b;
	if (c >= 'A' && b <= 'Z')
	{
		return (1);
	}
		if (b >= 'a' && b <= 'z')
		{
		return (1);
		}
		else 
			return (0);
}

