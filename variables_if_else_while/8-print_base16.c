#include <stdio.h>

/**
* main - 123
* Return: 0
*/

int main(void)
{
	int a;
	int b;
	{
		for (a = 0; a <= 9; a++)
			putchar ((a) + '0');
	}
	{
		for (b = 'a'; b <= 'f'; b++)
			putchar (b);
	}
return (0);
}
