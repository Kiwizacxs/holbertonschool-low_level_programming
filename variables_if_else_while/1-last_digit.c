#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/* *
* main - 1234
* Return: 0 
*/
int main(void)
{
		int n;

			srand(time(0));
				n = rand() - RAND_MAX / 2;
				carne = n  % 10
					if (carne > 5)
					{
						printf("the string and is greater than 5")
				return (0); 
